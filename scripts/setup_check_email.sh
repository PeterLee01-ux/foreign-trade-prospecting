# ================================================================ # ⚠️ DEPRECATED — 此脚本已废弃 # 请改用 scripts/install.sh 或 scripts/install.ps1 一键安装。 # 或直接运行： #   docker run -d --name reacher -p 8080:8080 --restart unless-stopped reacherhq/backend:latest # ================================================================ #!/bin/bash # 启动本地 check-if-email-exists (Reacher) Docker 邮箱验证服务 # 用法: bash setup_check_email.sh  set -e  echo "=== 外贸获客助手：邮箱验证服务部署 ===" echo ""  # 检查 Docker 是否安装 if ! command -v docker &> /dev/null; then     echo "[错误] 未找到 Docker。请先安装 Docker Desktop。"     echo "  Windows: https://docs.docker.com/desktop/install/windows-install/"     exit 1 fi  echo "[通过] Docker 已就绪"  # 停止已有的 reacher 容器 if docker ps -a --format '{{.Names}}' | grep -q '^reacher$'; then     echo "[信息] 正在移除旧的 reacher 容器..."     docker rm -f reacher 2>/dev/null fi  # 拉取最新镜像 echo "[信息] 正在拉取 reacherhq/backend:latest..." docker pull reacherhq/backend:latest  # 启动容器 echo "[信息] 正在 8080 端口启动 reacher..." docker run -d --name reacher -p 8080:8080 reacherhq/backend:latest  # 等待服务就绪 echo "[信息] 等待服务启动..." for i in {1..30}; do     if curl -s http://localhost:8080/health > /dev/null 2>&1; then         echo "[成功] Reacher 已运行在 http://localhost:8080"         echo ""         echo "快速测试："         curl -s "http://localhost:8080/v0/check_email?email=test@gmail.com"         exit 0     fi     sleep 1 done  echo "[警告] 容器已启动但健康检查超时。请检查: docker logs reacher"